Target Confirmation and Sampling

{% if proc.ultrasound_pattern %}{% set _view = (proc.ultrasound_pattern or "") | lower %}Radial EBUS: Radial EBUS verified a {{ _view }} {% if _view.endswith('view') %}{% else %}view{% endif %} relative to the lesion.{% endif %}
{% if proc.fluoro_used %}Imaging: Augmented fluoroscopy was used for trajectory confirmation.{% endif %}

{% if proc.sampling_tools or proc.passes_per_tool %}
Sampling: Sampling through the working channel was performed as follows:
{% if proc.passes_per_tool %}{{ proc.passes_per_tool }}{% else %}{{ proc.sampling_tools | join(', ') }}{% endif %}
{% endif %}

{% if proc.rose_result %}ROSE Results (Nodule): {{ proc.rose_result }}.{% endif %}
