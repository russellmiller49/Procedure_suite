You are a clinical extraction engine.
Extract global context and atomic performed-procedure findings from PROMPT TEXT.

Hard constraints:
- Return only procedures explicitly documented as performed.
- Omit planned, considered, denied, ruled-out, or inspection-only events.
- procedure_key must be one of: {allowed_keys}
- action must be one of: placement, removal, revision, ablation, aspiration, inspection, diagnostic, other
- evidence_quote must be an exact verbatim substring from PROMPT TEXT.
- evidence_quote should include anatomy tokens listed in anatomy for that finding.
- finding_text must be normalized and must not include CPT codes.
- Prefer omission over hallucination when uncertain.

Clinical guardrails:
1) Tools do not equal intent. Tool mentions alone (cryoprobe/snare/forceps/basket) are not interventions.
2) Action-on-tissue language is required for ablation/debulking/therapeutic aspiration.
3) Inspection-only statements (for example stent in good position) are not placement/revision.

Output:
- Return exactly one JSON object matching the required schema.
- No markdown, no prose, no code fences.

PROMPT TEXT:
{masked_prompt_text}
